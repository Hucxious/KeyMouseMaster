# “键鼠大师”Windows版软件需求与设计方案

## 一、项目概述

### 1.1 项目名称

键鼠大师（Key-Mouse-Master，KMM）。

### 1.2 项目定位

“键鼠大师”是一款面向 Windows 系统的桌面键鼠自动化软件，主要提供鼠标连续点击、键盘连续输入以及键鼠操作录制与回放功能。

软件主要适用于：

- 重复性鼠标点击；
- 重复性键盘操作；
- 软件功能测试；
- 办公流程自动化；
- 固定操作流程录制与复现；
- 多显示器环境下的跨屏键鼠自动化。

### 1.3 支持平台

软件仅支持：

- Windows 10 64位；
- Windows 11 64位。

暂不支持：

- Linux；
- macOS；
- Windows 32位系统；
- 移动端系统。

### 1.4 技术要求

| 项目         | 技术要求                           |
| ------------ | ---------------------------------- |
| 开发语言     | C++17                              |
| 界面框架     | Qt Widgets                         |
| Qt版本       | 推荐Qt 5.15或Qt 6.x                |
| 构建系统     | qmake                              |
| 工程文件     | `.pro`文件                         |
| 界面设计方式 | 使用C++代码手动创建控件和布局      |
| UI文件       | 不使用Qt Designer，不使用`.ui`文件 |
| 操作系统接口 | Windows API                        |
| 配置保存     | QSettings                          |
| 脚本存储     | JSON格式自定义宏文件               |
| 编码格式     | UTF-8                              |
| 默认架构     | x64                                |

------

# 二、总体功能范围

软件包含三个主要功能分页：

1. 鼠标连点；
2. 键盘连点；
3. 脚本录制与回放。

软件还需要提供以下公共功能：

- 全局启动快捷键；
- 全局停止快捷键；
- 紧急停止机制；
- 系统托盘；
- 参数自动保存；
- 脚本导入和导出；
- 多显示器识别；
- 多显示器坐标记录与映射；
- 高DPI缩放适配；
- 当前运行状态显示；
- 操作日志；
- 异常退出前释放全部键鼠状态。

同一时刻只允许执行一个自动化任务，包括：

- 鼠标连点；
- 键盘连点；
- 脚本录制；
- 脚本回放。

------

# 三、软件界面总体设计

## 3.1 主窗口

主窗口使用`QMainWindow`实现，主要包括：

- 顶部菜单栏；
- 中央分页区域；
- 底部状态栏；
- 系统托盘菜单。

中央分页区域使用`QTabWidget`，包含：

```text
鼠标连点 | 键盘连点 | 脚本录制
```

建议初始窗口尺寸：

```text
宽度：900 px
高度：650 px
最小尺寸：780 × 560 px
```

界面不得使用绝对坐标直接摆放控件，应使用：

- `QVBoxLayout`；
- `QHBoxLayout`；
- `QGridLayout`；
- `QFormLayout`；
- `QGroupBox`；
- `QSplitter`。

这样可以适配不同分辨率、不同字体大小和不同系统缩放比例。

## 3.2 菜单栏

建议菜单结构如下：

```text
文件
├── 导入脚本
├── 保存脚本
├── 脚本另存为
└── 退出

设置
├── 快捷键设置
├── 多显示器设置
├── 常规设置
└── 恢复默认设置

工具
├── 获取鼠标坐标
├── 显示器信息
└── 输入状态复位

帮助
├── 使用说明
├── 快捷键说明
└── 关于键鼠大师
```

## 3.3 状态栏

状态栏显示：

- 当前状态；
- 当前执行功能；
- 已执行次数；
- 当前鼠标所在显示器；
- 当前鼠标坐标；
- 紧急停止快捷键；
- 权限状态。

示例：

```text
状态：正在执行鼠标连点 | 次数：128/1000 |
显示器：DISPLAY2 | 坐标：(2350, 720) | 停止：F9
```

------

# 四、鼠标连点功能

## 4.1 功能目标

用户可选择鼠标按键、点击位置、点击间隔和执行次数，通过全局快捷键启动或停止连续点击。

## 4.2 鼠标按键设置

支持：

- 鼠标左键；
- 鼠标右键；
- 鼠标中键；
- 鼠标侧键1；
- 鼠标侧键2。

## 4.3 点击类型

支持：

- 单击；
- 双击；
- 仅按下；
- 仅释放；
- 长按。

其中长按模式应支持设置：

- 按下持续时间；
- 两次长按之间的间隔。

## 4.4 点击位置模式

支持三种模式。

### 模式一：实时鼠标位置

每次点击时读取当前鼠标所在位置。

适合用户一边移动鼠标，一边使用快捷键触发连点。

### 模式二：固定桌面坐标

启动时或配置时确定一个固定的虚拟桌面坐标。

用户可以：

- 手动输入X坐标；
- 手动输入Y坐标；
- 点击“获取当前位置”；
- 选择目标显示器；
- 在屏幕预览图中选择位置。

### 模式三：显示器相对坐标

保存目标显示器及其内部相对位置，例如：

```text
显示器：DISPLAY2
相对坐标：X=520，Y=360
```

当显示器整体位置发生变化时，仍然可以根据目标显示器重新计算桌面绝对坐标。

该模式比单纯保存虚拟桌面绝对坐标更适合多显示器环境。

## 4.5 执行参数

支持设置：

- 点击间隔；
- 间隔单位；
- 启动延迟；
- 执行次数；
- 无限循环；
- 按压时长；
- 双击间隔；
- 是否恢复启动前鼠标位置。

间隔单位至少支持：

- 毫秒；
- 秒。

建议参数范围：

| 参数     | 范围            |
| -------- | --------------- |
| 点击间隔 | 1～3,600,000 ms |
| 启动延迟 | 0～60,000 ms    |
| 执行次数 | 1～999,999,999  |
| 按压时长 | 1～60,000 ms    |

尽管允许输入1毫秒，但软件不保证操作系统实际能够稳定实现1毫秒级点击。

## 4.6 快捷键

支持：

- 启动快捷键；
- 停止快捷键；
- 启动/停止切换快捷键。

默认建议：

```text
启动：F8
停止：F9
```

快捷键可由用户自行修改，并支持组合键，例如：

```text
Ctrl + Shift + F8
Alt + F7
Ctrl + Alt + M
```

必须检查：

- 快捷键是否为空；
- 启动键与停止键是否冲突；
- 是否与其他分页快捷键冲突；
- 是否注册失败；
- 是否为系统保留快捷键。

------

# 五、键盘连点功能

## 5.1 功能目标

用户可选择一个普通按键或组合键，按照指定间隔和次数自动执行键盘输入。

## 5.2 按键类型

支持：

- A～Z；
- 0～9；
- F1～F24；
- 方向键；
- 空格；
- 回车；
- Tab；
- Esc；
- Backspace；
- Delete；
- Insert；
- Home；
- End；
- PageUp；
- PageDown；
- 小键盘按键；
- Ctrl；
- Shift；
- Alt；
- Win；
- 常用标点键。

## 5.3 组合键

支持一个主按键和多个修饰键。

例如：

```text
Ctrl + C
Ctrl + Shift + S
Alt + F4
Win + D
```

组合键执行顺序应为：

1. 按下修饰键；
2. 按下主按键；
3. 释放主按键；
4. 逆序释放修饰键。

## 5.4 输入模式

支持：

- 完整按键；
- 仅按下；
- 仅释放；
- 长按；
- 组合键；
- 单行文本输入。

第一版建议将“单行文本输入”作为可选功能。文本输入时，可逐字符模拟键盘输入，也可以提供剪贴板粘贴模式，但两种方式需要分别标识。

## 5.5 执行参数

支持：

- 按键间隔；
- 按压时长；
- 启动延迟；
- 执行次数；
- 无限循环；
- 启动快捷键；
- 停止快捷键。

必须满足：

```text
按压时长 < 按键间隔
```

若用户输入不符合要求，应禁止启动并给出明确提示。

## 5.6 键盘状态保护

程序停止或退出时，必须主动释放：

- Ctrl；
- Shift；
- Alt；
- Win；
- 当前自动按下的普通键。

程序需要维护当前已按下按键集合，防止出现按键未释放的问题。

------

# 六、脚本录制与回放

## 6.1 功能目标

软件能够监听用户的全局鼠标和键盘操作，将操作按时间顺序记录为脚本文件，并按照原始时间间隔或指定播放速度进行回放。

该文件可称为：

- 键鼠脚本；
- 宏脚本；
- 宏文件。

软件界面统一使用“脚本”这一名称。

## 6.2 脚本分页结构

建议采用左右分栏布局。

### 左侧：脚本管理区

包括：

- 新建脚本；
- 导入脚本；
- 保存脚本；
- 脚本另存为；
- 删除脚本；
- 最近脚本；
- 脚本名称；
- 脚本说明。

### 右侧：事件列表区

使用`QTableView`显示脚本事件。

建议列内容：

| 列           | 内容               |
| ------------ | ------------------ |
| 序号         | 事件编号           |
| 时间         | 相对录制开始时刻   |
| 事件类型     | 鼠标或键盘事件     |
| 按键/按钮    | 键盘按键或鼠标按钮 |
| 显示器       | 事件所在显示器     |
| 桌面坐标     | 虚拟桌面坐标       |
| 显示器内坐标 | 相对目标显示器坐标 |
| 参数         | 滚轮、按压等信息   |
| 启用状态     | 是否执行该事件     |

## 6.3 可录制事件

### 鼠标事件

支持录制：

- 鼠标移动；
- 左键按下；
- 左键释放；
- 右键按下；
- 右键释放；
- 中键按下；
- 中键释放；
- 侧键按下；
- 侧键释放；
- 垂直滚轮；
- 水平滚轮。

### 键盘事件

支持录制：

- 普通键按下；
- 普通键释放；
- 修饰键按下；
- 修饰键释放；
- 功能键；
- 方向键；
- 小键盘按键；
- 常用系统键。

## 6.4 录制参数

支持设置：

- 录制开始快捷键；
- 录制停止快捷键；
- 是否录制鼠标移动；
- 是否录制鼠标点击；
- 是否录制滚轮；
- 是否录制键盘；
- 鼠标移动最小记录间隔；
- 鼠标移动最小记录距离；
- 是否忽略软件自身窗口内操作；
- 是否录制软件模拟生成的事件。

默认情况下：

```text
不记录软件模拟生成的键鼠事件
```

避免回放事件被再次录制。

## 6.5 鼠标移动压缩

鼠标移动会产生大量高频事件，因此需要进行压缩。

建议规则：

- 两次事件时间间隔小于10 ms时合并；
- 两次位置距离小于2 px时忽略；
- 鼠标按下前的最后一个位置必须保留；
- 鼠标释放前的最后一个位置必须保留；
- 拖动操作中的移动事件不得全部删除；
- 可选用轨迹关键点压缩算法。

## 6.6 脚本回放设置

支持：

- 执行快捷键；
- 停止快捷键；
- 重复次数；
- 无限重复；
- 启动延迟；
- 每轮间隔；
- 播放速度；
- 坐标映射方式；
- 回放前检查显示器配置；
- 回放完成后恢复鼠标位置。

播放速度支持：

```text
0.25倍
0.5倍
0.75倍
1.0倍
1.5倍
2.0倍
自定义
```

## 6.7 脚本编辑功能

第一版至少支持：

- 删除事件；
- 禁用事件；
- 修改事件时间；
- 修改鼠标坐标；
- 修改目标显示器；
- 修改键盘按键；
- 插入等待事件；
- 上移事件；
- 下移事件；
- 复制事件；
- 清空事件；
- 从选中位置开始测试执行。

暂不实现：

- 条件判断；
- 图像识别；
- 流程跳转；
- 变量；
- 表达式；
- 循环嵌套；
- 窗口控件识别；
- 网络操作。

------

# 七、多显示器支持方案

## 7.1 多显示器基本要求

软件必须正确支持：

- 主显示器与副显示器；
- 横向排列；
- 纵向排列；
- 左侧副屏；
- 右侧副屏；
- 上方或下方副屏；
- 不同分辨率；
- 不同缩放比例；
- 显示器热插拔；
- 主显示器切换；
- 显示器排列顺序变化；
- 显示器负坐标。

## 7.2 Windows虚拟桌面坐标

Windows将所有显示器组合为一个虚拟桌面。

例如：

```text
副屏位于主屏左侧：

副屏：X=-1920～-1
主屏：X=0～1919
```

因此鼠标坐标可能为负数。

软件中的坐标数据类型不得使用无符号整数，应使用：

```cpp
int
qint32
QPoint
QRect
```

禁止使用：

```cpp
unsigned int
```

保存鼠标坐标。

## 7.3 显示器信息

程序启动时应读取所有显示器：

- 显示器编号；
- 系统设备名称；
- 是否为主显示器；
- 桌面几何区域；
- 可用区域；
- 物理尺寸；
- 分辨率；
- DPI；
- 缩放比例；
- 刷新率；
- 显示器相对位置。

显示器可以在界面中显示为：

```text
显示器1（主显示器）
1920 × 1080
位置：(0, 0)
缩放：125%

显示器2
2560 × 1440
位置：(1920, -180)
缩放：100%
```

## 7.4 坐标保存方式

脚本中建议同时保存三类坐标。

### 1. 虚拟桌面绝对坐标

例如：

```text
X=2350
Y=720
```

### 2. 显示器内部坐标

例如：

```text
显示器：DISPLAY2
X=430
Y=720
```

### 3. 显示器比例坐标

例如：

```text
X比例=0.168
Y比例=0.500
```

这样可以在显示器分辨率发生变化时，按照比例重新定位。

## 7.5 坐标回放模式

用户可以选择以下模式。

### 精确模式

使用虚拟桌面绝对坐标。

适用于：

- 显示器配置没有变化；
- 分辨率和排列方式保持一致。

### 显示器绑定模式

根据目标显示器设备信息和显示器内部坐标进行回放。

适用于：

- 显示器整体位置发生变化；
- 主副屏排列发生变化；
- 显示器仍然存在。

### 比例缩放模式

根据目标显示器当前分辨率，对录制坐标按比例缩放。

适用于：

- 显示器分辨率发生变化；
- 录制和回放时缩放比例不同。

建议默认使用：

```text
显示器绑定模式
```

## 7.6 显示器身份识别

不能只使用显示器序号进行匹配，因为Windows中的显示器序号可能发生变化。

建议优先使用：

1. 显示器设备名称；
2. 显示器设备路径；
3. EDID相关标识；
4. 设备分辨率；
5. 相对排列位置；
6. 用户手动映射。

若找不到原显示器，应弹出映射对话框：

```text
录制时的显示器“DISPLAY2”当前不存在。
请选择用于替代的显示器。
```

## 7.7 显示器配置变化检测

软件应监听以下变化：

- 显示器接入；
- 显示器断开；
- 分辨率变化；
- 缩放比例变化；
- 主显示器变化；
- 排列方式变化。

检测到变化后：

- 刷新显示器列表；
- 重新计算虚拟桌面区域；
- 暂停正在等待启动的任务；
- 若正在执行脚本，可根据用户设置选择停止或继续；
- 提示用户重新检查坐标映射。

------

# 八、高DPI与缩放处理

## 8.1 问题说明

Windows多显示器可能具有不同缩放比例，例如：

```text
显示器1：1920×1080，125%
显示器2：2560×1440，100%
```

Qt界面坐标与Windows物理像素坐标可能不完全一致，因此不能直接混用。

## 8.2 坐标层次

程序内部应明确区分：

- Qt逻辑坐标；
- Windows屏幕物理坐标；
- 显示器内部坐标；
- 虚拟桌面坐标；
- 比例坐标。

输入模拟接口最终应使用Windows所要求的坐标体系。

## 8.3 DPI模式

程序应启用每显示器DPI感知，优先使用：

```text
Per-Monitor DPI Aware V2
```

这样窗口从一个显示器移动到另一个显示器时，可以正确适配新的缩放比例。

## 8.4 坐标转换模块

建议单独设计：

```text
CoordinateMapper
```

负责：

- Qt逻辑坐标转物理坐标；
- 物理坐标转虚拟桌面坐标；
- 虚拟桌面坐标转显示器相对坐标；
- 相对坐标转当前显示器坐标；
- 比例坐标映射；
- 坐标边界检查；
- 显示器缺失处理。

界面层和脚本播放器不得自行计算坐标。

------

# 九、Windows系统功能实现

## 9.1 全局快捷键

使用Windows全局快捷键机制。

主要功能：

- 注册快捷键；
- 注销快捷键；
- 接收快捷键消息；
- 检查快捷键冲突；
- 程序退出时释放快捷键。

Qt中可通过原生事件过滤器处理Windows消息。

## 9.2 全局键鼠监听

脚本录制使用Windows低级钩子：

- 低级键盘钩子；
- 低级鼠标钩子。

钩子回调中不应执行耗时操作，只负责：

1. 读取事件；
2. 标记事件时间；
3. 写入线程安全队列；
4. 立即返回。

事件格式转换、压缩和界面更新应在其他线程中完成。

## 9.3 键鼠输入模拟

使用Windows输入模拟接口完成：

- 鼠标移动；
- 鼠标按下；
- 鼠标释放；
- 鼠标滚轮；
- 键盘按下；
- 键盘释放；
- 组合键。

模拟事件应带有软件自定义标记，录制钩子可以据此识别并忽略软件自身生成的输入。

## 9.4 权限问题

普通权限运行的软件不能可靠控制以管理员权限运行的目标程序。

软件应检测并提示：

```text
目标程序可能以更高权限运行。
请尝试以管理员身份启动键鼠大师。
```

不建议软件默认要求管理员权限，而应在必要时由用户手动选择。

------

# 十、软件架构

## 10.1 分层结构

建议采用以下分层：

```text
界面层
    ↓
业务控制层
    ↓
功能核心层
    ↓
Windows平台层
```

## 10.2 界面层

主要类：

- `MainWindow`；
- `MouseClickPage`；
- `KeyboardClickPage`；
- `ScriptPage`；
- `HotkeyEdit`；
- `MonitorPreviewWidget`；
- `ScriptEventTableModel`。

职责：

- 创建控件；
- 收集用户输入；
- 参数校验；
- 显示状态；
- 发送业务命令。

## 10.3 业务控制层

主要类：

- `AppController`；
- `TaskManager`；
- `HotkeyManager`；
- `SettingsManager`。

职责：

- 控制功能启动和停止；
- 防止任务冲突；
- 管理软件状态；
- 保存配置；
- 处理全局快捷键；
- 处理异常状态。

## 10.4 核心功能层

主要类：

- `MouseClickEngine`；
- `KeyboardClickEngine`；
- `ScriptRecorder`；
- `ScriptPlayer`；
- `ScriptSerializer`；
- `CoordinateMapper`；
- `MonitorManager`；
- `InputStateManager`。

## 10.5 Windows平台层

主要类：

- `WindowsInputSimulator`；
- `WindowsHookManager`；
- `WindowsHotkeyBackend`；
- `WindowsMonitorBackend`；
- `WindowsDpiManager`。

Windows API调用应集中在平台层，不应散落在界面代码中。

------

# 十一、运行状态设计

建议定义统一状态：

```text
Idle
Preparing
MouseClicking
KeyboardClicking
Recording
Playing
Paused
Stopping
Completed
Error
```

状态约束如下：

- 空闲状态可以启动任意任务；
- 录制状态不能启动回放；
- 回放状态不能修改当前脚本；
- 连点状态不能开始录制；
- 停止过程中不能再次启动；
- 错误状态必须完成输入复位后才能回到空闲。

所有页面共用一个统一的任务状态，避免多个页面同时工作。

------

# 十二、多线程设计

## 12.1 主线程

主线程负责：

- Qt界面；
- 菜单与窗口操作；
- 状态显示；
- 用户参数输入；
- Windows消息转发。

主线程不得执行长时间循环。

## 12.2 工作线程

建议设置：

- 连点工作线程；
- 脚本录制处理线程；
- 脚本回放线程；
- 日志写入线程。

实际项目中鼠标和键盘连点可以共用一个自动化工作线程。

## 12.3 停止控制

使用原子变量和条件变量：

```cpp
std::atomic_bool stopRequested;
std::atomic_bool pauseRequested;
```

等待过程中必须允许立即停止，不能使用无法中断的长时间休眠。

## 12.4 时间控制

使用高精度计时器保存相对时间。

脚本事件使用相对于录制开始时刻的绝对时间：

```text
事件1：0 ms
事件2：132 ms
事件3：187 ms
事件4：630 ms
```

回放时根据目标时间计算剩余等待时间，减少累计误差。

------

# 十三、脚本文件格式

## 13.1 文件扩展名

建议使用：

```text
.kms
```

含义为：

```text
Key Mouse Script
```

文件内部采用JSON格式。

## 13.2 文件内容

脚本文件包含：

- 文件格式名称；
- 版本号；
- 脚本名称；
- 创建时间；
- 修改时间；
- 录制时系统信息；
- 录制时显示器信息；
- 坐标模式；
- 事件列表；
- 默认播放参数。

示例结构：

```json
{
  "format": "KeyMouseScript",
  "version": 1,
  "name": "示例脚本",
  "created_at": "2026-07-20T17:00:00",
  "coordinate_mode": "monitor_relative",
  "desktop": {
    "left": -1920,
    "top": 0,
    "width": 4480,
    "height": 1440
  },
  "monitors": [
    {
      "id": "DISPLAY1",
      "primary": true,
      "x": 0,
      "y": 0,
      "width": 2560,
      "height": 1440,
      "scale": 1.0
    },
    {
      "id": "DISPLAY2",
      "primary": false,
      "x": -1920,
      "y": 360,
      "width": 1920,
      "height": 1080,
      "scale": 1.25
    }
  ],
  "events": [
    {
      "time_ms": 0,
      "type": "mouse_move",
      "monitor_id": "DISPLAY2",
      "desktop_x": -850,
      "desktop_y": 720,
      "monitor_x": 1070,
      "monitor_y": 360,
      "ratio_x": 0.5573,
      "ratio_y": 0.3333
    },
    {
      "time_ms": 150,
      "type": "mouse_press",
      "button": "left"
    },
    {
      "time_ms": 210,
      "type": "mouse_release",
      "button": "left"
    }
  ]
}
```

## 13.3 文件校验

导入脚本时应检查：

- JSON是否合法；
- 文件格式名称是否正确；
- 版本号是否支持；
- 事件类型是否合法；
- 时间戳是否递增；
- 坐标是否有效；
- 显示器是否存在；
- 键码是否能够映射；
- 事件数量是否过大。

------

# 十四、配置保存

使用`QSettings`保存：

- 窗口位置；
- 窗口尺寸；
- 当前分页；
- 鼠标连点参数；
- 键盘连点参数；
- 各功能快捷键；
- 最近脚本列表；
- 默认脚本目录；
- 多显示器坐标模式；
- 系统托盘设置；
- 日志等级；
- 是否开机启动。

脚本文件和软件配置应分开存储。

------

# 十五、系统托盘功能

关闭主窗口时可根据用户设置执行：

- 直接退出；
- 最小化到系统托盘；
- 弹出确认框。

托盘菜单建议包含：

```text
显示主窗口
启动当前任务
停止当前任务
紧急释放全部按键
退出
```

托盘图标根据状态变化：

- 空闲；
- 正在运行；
- 正在录制；
- 已暂停；
- 发生错误。

------

# 十六、安全与异常处理

## 16.1 紧急停止

必须提供一个全局紧急停止快捷键。

紧急停止应执行：

1. 立即设置停止标志；
2. 唤醒所有等待线程；
3. 停止后续输入；
4. 释放所有键盘按键；
5. 释放所有鼠标按钮；
6. 停止录制或回放；
7. 恢复界面状态；
8. 记录停止原因。

## 16.2 输入复位

设置“输入状态复位”功能，用于主动释放：

- Ctrl；
- Shift；
- Alt；
- Win；
- 鼠标左键；
- 鼠标右键；
- 鼠标中键；
- 侧键。

## 16.3 坐标越界

脚本事件坐标超出当前虚拟桌面时，可以提供三种策略：

- 停止执行；
- 自动限制到最近边界；
- 跳过该事件。

默认建议：

```text
停止执行并提示用户重新映射显示器
```

## 16.4 显示器断开

脚本回放过程中若目标显示器断开：

- 立即暂停回放；
- 禁止继续发送鼠标移动；
- 弹出显示器重新映射窗口；
- 用户确认后继续或终止。

## 16.5 程序退出

退出程序前必须：

- 停止当前任务；
- 注销全局快捷键；
- 卸载全局钩子；
- 释放全部键鼠状态；
- 保存配置；
- 等待工作线程退出。

------

# 十七、项目目录结构

```text
KeyMouseMaster/
├── KeyMouseMaster.pro
├── README.md
├── LICENSE
│
├── resources/
│   ├── resources.qrc
│   ├── icons/
│   └── styles/
│
├── src/
│   ├── main.cpp
│   │
│   ├── ui/
│   │   ├── MainWindow.h
│   │   ├── MainWindow.cpp
│   │   ├── MouseClickPage.h
│   │   ├── MouseClickPage.cpp
│   │   ├── KeyboardClickPage.h
│   │   ├── KeyboardClickPage.cpp
│   │   ├── ScriptPage.h
│   │   ├── ScriptPage.cpp
│   │   ├── HotkeyEdit.h
│   │   ├── HotkeyEdit.cpp
│   │   ├── MonitorPreviewWidget.h
│   │   └── MonitorPreviewWidget.cpp
│   │
│   ├── core/
│   │   ├── AppController.h
│   │   ├── AppController.cpp
│   │   ├── TaskManager.h
│   │   ├── TaskManager.cpp
│   │   ├── MouseClickEngine.h
│   │   ├── MouseClickEngine.cpp
│   │   ├── KeyboardClickEngine.h
│   │   ├── KeyboardClickEngine.cpp
│   │   ├── ScriptRecorder.h
│   │   ├── ScriptRecorder.cpp
│   │   ├── ScriptPlayer.h
│   │   ├── ScriptPlayer.cpp
│   │   ├── ScriptSerializer.h
│   │   ├── ScriptSerializer.cpp
│   │   ├── ScriptEvent.h
│   │   ├── ScriptEvent.cpp
│   │   ├── MonitorManager.h
│   │   ├── MonitorManager.cpp
│   │   ├── CoordinateMapper.h
│   │   ├── CoordinateMapper.cpp
│   │   ├── InputStateManager.h
│   │   └── InputStateManager.cpp
│   │
│   ├── platform/
│   │   └── windows/
│   │       ├── WindowsInputSimulator.h
│   │       ├── WindowsInputSimulator.cpp
│   │       ├── WindowsHookManager.h
│   │       ├── WindowsHookManager.cpp
│   │       ├── WindowsHotkeyManager.h
│   │       ├── WindowsHotkeyManager.cpp
│   │       ├── WindowsMonitorBackend.h
│   │       ├── WindowsMonitorBackend.cpp
│   │       ├── WindowsDpiManager.h
│   │       └── WindowsDpiManager.cpp
│   │
│   ├── settings/
│   │   ├── SettingsManager.h
│   │   └── SettingsManager.cpp
│   │
│   ├── models/
│   │   ├── ScriptEventTableModel.h
│   │   └── ScriptEventTableModel.cpp
│   │
│   └── utils/
│       ├── Logger.h
│       ├── Logger.cpp
│       ├── KeyMapper.h
│       ├── KeyMapper.cpp
│       ├── TimeUtils.h
│       └── TimeUtils.cpp
│
├── scripts/
├── tests/
└── build/
```

------

# 十八、qmake工程要求

`.pro`文件至少包含：

```text
QT += core gui widgets
CONFIG += c++17
TEMPLATE = app
```

Windows系统库按需要加入：

```text
user32
shell32
advapi32
shcore
```

工程中不得包含：

- CMakeLists.txt；
- `.ui`文件；
- Linux平台代码；
- macOS平台代码。

Debug和Release输出目录应分离。

建议目录：

```text
build/debug
build/release
```

------

# 十九、第一版功能范围

## 19.1 第一版必须完成

### 公共功能

- Windows 10和Windows 11运行；
- qmake编译；
- 纯C++界面；
- 三个功能分页；
- 全局快捷键；
- 紧急停止；
- 配置保存；
- 系统托盘；
- 日志记录；
- 多显示器识别；
- 高DPI适配；
- 显示器变化检测。

### 鼠标连点

- 左键、右键、中键；
- 固定位置；
- 当前鼠标位置；
- 显示器相对位置；
- 点击间隔；
- 启动延迟；
- 执行次数；
- 无限循环；
- 启动和停止快捷键。

### 键盘连点

- 普通按键；
- 功能键；
- 组合键；
- 按键间隔；
- 按压时长；
- 启动延迟；
- 执行次数；
- 无限循环；
- 启动和停止快捷键。

### 脚本功能

- 全局鼠标录制；
- 全局键盘录制；
- 脚本保存；
- 脚本导入；
- 脚本回放；
- 重复次数；
- 播放速度；
- 启动和停止快捷键；
- 脚本事件表格；
- 删除和禁用事件；
- 显示器坐标映射；
- 显示器缺失提示。

## 19.2 第一版暂不完成

- 图像识别；
- OCR；
- 窗口控件自动识别；
- 条件判断；
- 流程跳转；
- 脚本变量；
- 脚本编程语言；
- 驱动级输入；
- 游戏反检测；
- 网络远程控制；
- 多任务并行执行；
- 云端脚本同步；
- 自动更新。

------

# 二十、开发阶段规划

## 第一阶段：工程与界面

完成：

- qmake工程；
- 主窗口；
- 三个分页；
- 纯代码布局；
- 参数控件；
- 自定义快捷键输入控件；
- 状态栏；
- 系统托盘；
- 参数校验；
- 配置保存。

## 第二阶段：鼠标和键盘连点

完成：

- Windows输入模拟；
- 鼠标连点；
- 键盘连点；
- 全局快捷键；
- 工作线程；
- 停止控制；
- 输入状态复位。

## 第三阶段：脚本录制

完成：

- 全局鼠标钩子；
- 全局键盘钩子；
- 事件数据模型；
- 时间戳记录；
- 模拟事件过滤；
- 鼠标移动压缩；
- 脚本事件列表。

## 第四阶段：脚本回放

完成：

- 脚本解析；
- 脚本播放；
- 播放速度；
- 重复执行；
- 暂停和停止；
- 时间误差校正；
- 异常输入释放。

## 第五阶段：多显示器适配

完成：

- 显示器枚举；
- 虚拟桌面坐标；
- 负坐标处理；
- 显示器相对坐标；
- 坐标比例映射；
- 高DPI适配；
- 显示器热插拔；
- 显示器重新映射界面。

## 第六阶段：测试与打包

完成：

- Windows 10测试；
- Windows 11测试；
- 单显示器测试；
- 双显示器测试；
- 三显示器测试；
- 不同DPI测试；
- 管理员权限测试；
- 异常退出测试；
- Release打包；
- Qt运行库部署；
- 使用说明编写。

------

# 二十一、验收标准

## 21.1 基础验收

- 软件能够通过qmake正常编译；
- 工程中不包含`.ui`文件；
- 软件能够在Windows 10和Windows 11启动；
- 三个分页能够正常切换；
- 配置能够保存并恢复。

## 21.2 鼠标连点验收

- 能够选择鼠标按键；
- 能够设置点击间隔；
- 能够设置执行次数；
- 能够通过全局快捷键启动；
- 能够通过全局快捷键停止；
- 能够在副显示器固定位置点击；
- 能够处理负坐标；
- 停止后不存在鼠标按键未释放问题。

## 21.3 键盘连点验收

- 能够执行单键；
- 能够执行组合键；
- 能够设置按压时长；
- 能够设置执行次数；
- 能够正常启动和停止；
- 停止后不存在Ctrl、Shift、Alt等按键未释放问题。

## 21.4 脚本录制验收

- 能够记录鼠标点击；
- 能够记录鼠标移动；
- 能够记录鼠标滚轮；
- 能够记录键盘按下和释放；
- 能够正确记录事件时间；
- 能够区分不同显示器；
- 能够过滤软件自身模拟事件。

## 21.5 脚本回放验收

- 能够导入脚本；
- 能够按照事件顺序执行；
- 能够按照录制时间回放；
- 能够调整播放速度；
- 能够设置重复次数；
- 能够使用全局快捷键停止；
- 多显示器排列未变化时可准确回放；
- 显示器位置变化后可通过重新映射继续回放。

## 21.6 多显示器验收

至少测试以下场景：

1. 单显示器；
2. 副屏位于主屏右侧；
3. 副屏位于主屏左侧；
4. 副屏位于主屏上方；
5. 两个显示器分辨率不同；
6. 两个显示器缩放比例不同；
7. 主显示器切换；
8. 录制后更改显示器排列；
9. 录制后断开一个显示器；
10. 软件运行过程中接入新显示器。

------

# 二十二、最终方案结论

Windows专用版本不再需要处理Linux X11、Wayland以及跨平台系统接口差异，整体开发难度明显降低。

项目的重点应放在以下几个方面：

1. Windows全局键鼠监听；
2. Windows键鼠输入模拟；
3. 全局快捷键；
4. 多显示器虚拟桌面坐标；
5. 负坐标处理；
6. 不同显示器DPI缩放；
7. 显示器变化后的坐标重新映射；
8. 脚本执行时间精度；
9. 输入状态安全释放；
10. 异常情况下的紧急停止。

推荐采用：

```text
统一业务逻辑
+
独立Windows平台模块
+
专门的显示器管理模块
+
专门的坐标转换模块
```

多显示器功能不能只作为鼠标连点页面中的一个附加选项，而应作为整个软件的基础能力。鼠标连点、脚本录制和脚本回放都应统一通过`MonitorManager`和`CoordinateMapper`处理显示器与坐标，避免后续出现各模块坐标规则不一致的问题。